<?xml version="1.0" encoding="utf-8"?>
<!--
  2x1 projects widget. Left cell: mic -> Kimi. Right cell: up to two
  project pills + a three-dot settings affordance in the top-right corner.

  RemoteViews only supports a limited widget set (LinearLayout, FrameLayout,
  TextView, ImageView, ...) — a plain <View> here once made the launcher
  refuse to add the widget. LayoutContractTest now pins the allowlist.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal"
    android:weightSum="2"
    android:background="@drawable/widget_bg"
    android:paddingLeft="4dp"
    android:paddingRight="4dp">

    <!-- Left 1x1: open Kimi -->
    <LinearLayout
        android:id="@+id/widget_btn_kimi"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:orientation="horizontal"
        android:gravity="center">

        <ImageView
            android:layout_width="26dp"
            android:layout_height="26dp"
            android:src="@drawable/ic_k"
            android:contentDescription="@string/widget_label" />
    </LinearLayout>

    <!-- Divider: a TextView, NOT a View — View is not RemoteViews-safe. -->
    <TextView
        android:layout_width="1dp"
        android:layout_height="24dp"
        android:layout_gravity="center_vertical"
        android:background="@color/divider_soft" />

    <!-- Right 1x1: project pills + settings affordance -->
    <FrameLayout
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:gravity="center"
            android:paddingLeft="6dp"
            android:paddingRight="22dp">

            <TextView
                android:id="@+id/pill_1"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@drawable/pill_bg"
                android:gravity="center"
                android:paddingLeft="10dp"
                android:paddingRight="10dp"
                android:paddingTop="3dp"
                android:paddingBottom="3dp"
                android:singleLine="true"
                android:ellipsize="end"
                android:textColor="@color/warm_brown"
                android:textSize="11sp" />

            <TextView
                android:id="@+id/pill_2"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:background="@drawable/pill_bg"
                android:gravity="center"
                android:paddingLeft="10dp"
                android:paddingRight="10dp"
                android:paddingTop="3dp"
                android:paddingBottom="3dp"
                android:singleLine="true"
                android:ellipsize="end"
                android:textColor="@color/warm_brown"
                android:textSize="11sp" />
        </LinearLayout>

        <ImageView
            android:id="@+id/widget_btn_settings"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_gravity="top|right"
            android:padding="6dp"
            android:src="@drawable/ic_more"
            android:contentDescription="@string/settings_open" />
    </FrameLayout>
</LinearLayout>
