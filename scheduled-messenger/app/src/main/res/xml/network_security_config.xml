<?xml version="1.0" encoding="utf-8"?>
<!-- Carrier MMS servers (MMSC) are plain http; the app makes no other network requests. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
